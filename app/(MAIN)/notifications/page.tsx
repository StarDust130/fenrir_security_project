import ComingSoon from "@/components/ComingSoon";

export const metadata = { title: "Notifications — aps" };

export default function NotificationsPage() {
  return <ComingSoon pageName="Notifications" />;
}
